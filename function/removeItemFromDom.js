/**
 * 
 * @param {string} id 
 * @param {string} item 
 * @param {HTMLElement} parent 
 */

export function removeItemFromDom(id , item , parent){
    let element = document.querySelector(`id`);
    
    parent.removeChild(element)
}